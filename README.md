Most of the projects are done *outside of the STM32CubeIDE* and the toolchand of choise is CMake

```make
cmake --preset Debug
cmake --build --preset Debug
```

Forther more i used openocd to flash and debug the firmware.
